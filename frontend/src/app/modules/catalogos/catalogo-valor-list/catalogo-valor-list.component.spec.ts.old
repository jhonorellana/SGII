import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CatalogoValorListComponent } from './catalogo-valor-list.component';

describe('CatalogoValorListComponent', () => {
  let component: CatalogoValorListComponent;
  let fixture: ComponentFixture<CatalogoValorListComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [CatalogoValorListComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(CatalogoValorListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
